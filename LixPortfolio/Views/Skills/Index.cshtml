@model IEnumerable<LixPortfolio.Models.Skill>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Skill
        </th>
        <th>
           Description
        </th>
       
        <th>
           Skill Type
        </th>
        <th></th>
    </tr>

@if(Model!= null) { 
foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Skill1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Skill_Description)
        </td>
      
        <td>
            @Html.DisplayFor(modelItem => item.SkillType1.SkillType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}
    }
</table>
